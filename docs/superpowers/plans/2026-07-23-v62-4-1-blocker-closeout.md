# AlphaPilot V62.4.1 Blocker Closeout Plan

## Phase 1: Evidence Truth

1. Add failing tests for blocking-state derivation and candidate attribution.
2. Replace the unconditional V62.4 acceptance label with evidence-derived
   V62.4.1 status.
3. Emit concrete failure attribution for every non-selected candidate.
4. Split open issues into blocking P1 and non-blocking P2 records.
5. Run focused tests and commit the phase.

## Phase 2: Runtime and Matchability

1. Add no-order runtime identity and lease capture.
2. Add 1h/1d shadow parity and reconciliation artifacts.
3. Add 30d/90d and broad-universe matchability audits.
4. Prove Demo ARM false, order count zero, Live false, and Withdraw false.
5. Run focused tests and commit the phase.

## Phase 3: Independent Validation and UI

1. Replace verifier wrappers with independent semantic implementations.
2. Add mutation, disconnect, security, static-analysis, and Playwright gates.
3. Project current pilot and AI workflow truth in the primary UI.
4. Exercise a real historical-failure dual-model critic workflow without
   granting the AI worker exchange credentials or execution permissions.
5. Run focused and full tests and commit the phase.

## Phase 4: Formal and Delta Handoff

1. Freeze a fresh preregistration and data snapshot for
   `v35_tsmom_crypto_adaptation`.
2. Verify `formalRunCount=0` and `resultReadCount=0` before execution.
3. Run the formal campaign once.
4. Publish the result without tuning the frozen identity.
5. Build and independently verify the V62.4.1 Delta Acceptance ZIP.
6. Run compile, config, safety, diff, repository, and package checks.
7. Commit, tag, push, verify remote SHAs, and report the exact mechanical state.

